### INITIAL VALUES
#dt:
#  default_value: 0.05
#  low_limit: 0.05
#  high_limit: 1.0
#  direction: 'positive'
#
#mass:
#  default_value: 1.0
#  low_limit: 1.0
#  high_limit: 5.0
#  direction: 'positive'
#
#length:
#  default_value: 1.0
#  low_limit: 1.0
#  high_limit: 10.0
#  direction: 'positive'

# VALUES AFTER FINDING MULTIPLIERS AUTOMATICALLY (AVERAGED OVER 3 RUNS)
# continual_learning_range = drop_range * 4, drop_range = [default_value, default_value * starting_multiplier] or
# drop_range = [default_value * starting_multiplier, default_value] if starting_multiplier > 1.0. To uniform the
# ranges of parameters between algorithms we take the maximum limit for a param in the case in which
# starting_multipliers are different
dt:
  default_value: 0.05
  low_limit: 0.05
  high_limit: 0.4
  starting_multiplier: 2.0
  percentage_drop: 70.58
  direction: 'positive'

mass:
  default_value: 1.0
  low_limit: 1.0
  high_limit: 5.16
  starting_multiplier: 1.29
  percentage_drop: 32.95
  direction: 'positive'

length:
  default_value: 1.0
  low_limit: 1.0
  high_limit: 21.44
  starting_multiplier: 5.36
  percentage_drop: 90.40
  direction: 'positive'
