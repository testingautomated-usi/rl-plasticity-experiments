### INITIAL VALUES
#dt:
#  default_value: 0.05
#  low_limit: 0.05
#  high_limit: 1.0
#  direction: 'positive'
#
#mass:
#  default_value: 1.0
#  low_limit: 1.0
#  high_limit: 5.0
#  direction: 'positive'
#
#length:
#  default_value: 1.0
#  low_limit: 1.0
#  high_limit: 10.0
#  direction: 'positive'

# VALUES AFTER FINDING MULTIPLIERS AUTOMATICALLY (AVERAGED OVER 3 RUNS)
# continual_learning_range = drop_range * 4, drop_range = [default_value, default_value * starting_multiplier] or
# drop_range = [default_value * starting_multiplier, default_value] if starting_multiplier > 1.0. To uniform the
# ranges of parameters between algorithms we take the maximum limit for a param in the case in which
# starting_multipliers are different
dt:
  default_value: 0.05
  low_limit: 0.05
  high_limit: 0.332
  starting_multiplier: 1.66
  percentage_drop: 67.52
  direction: 'positive'

mass:
  default_value: 1.0
  low_limit: 1.0
  high_limit: 4.72
  starting_multiplier: 1.18
  percentage_drop: 73.32
  direction: 'positive'

length:
  default_value: 1.0
  low_limit: 1.0
  high_limit: 13.84
  starting_multiplier: 3.46
  percentage_drop: 49.21
  direction: 'positive'
